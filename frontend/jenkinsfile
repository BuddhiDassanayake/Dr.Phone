pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/BuddhiDassanayake/Dr.Phone.git',
                    credentialsId: 'github-token'
            }
        }
    }
}